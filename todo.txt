TODOs:
- bulk actions select all checkbox does not select all, bulk delete isn't implemented.
- simplify useFlags hook
- flag controller duplicated (one of them is for debug purposes)
- remove `backend/database/migrations/2026_01_31_210909_add_uuid_to_feature_flags.php` migration and just roll it into the create_users_table migration

- create script to launch the whole app
- script to run all tests

wontfix:
- password reset isn't implemented